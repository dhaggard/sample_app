<!DOCTYPE html>
<html>
	<head>
		<% provide(:title, "Home") %>
	</head>

	<body>
    <% if logged_in? %>
      <%= render "shared/logged_in" %>
    <% else %>
      <%= render "shared/not_logged_in" %>
    <% end %>
	</body>
	
</html>